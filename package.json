{
  "name": "root",
  "private": true,
  "packageManager": "pnpm@11.13.1",
  "scripts": {
    "typecheck": "npx -y pnpm@11 -r --parallel run typecheck",
    "test": "npx -y pnpm@11 -r --parallel run test",
    "build": "npx -y pnpm@11 -r run build",
    "release": "npx -y pnpm@11 run build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^3.0.0-next.5"
  }
}
