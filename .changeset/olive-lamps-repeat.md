---
"disk": minor
---

Add the sandbox API: `archil.sandbox.create/get/list/start` plus `Sandbox.run/stop/start/refresh`, using the generated control-plane contract for long-lived `/api/sandboxes` microVMs with persistent root disks.
