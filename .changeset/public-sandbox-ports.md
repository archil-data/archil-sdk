---
"disk": minor
---

Add public sandbox ports: pass `ports` at creation, or use `exposePort()`, `listPorts()`, and `unexposePort()` on an existing sandbox. Expose and list return the port number and public hostname.
