---
"disk": minor
---

Add shared named CLI profiles with protected credential storage, ship a standalone `sandbox` executable for sandbox inspection, creation, lifecycle operations, one-shot commands, and interactive shells, and expose sandbox port mappings in the Node SDK.
