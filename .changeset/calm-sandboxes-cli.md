---
"disk": minor
---

Add shared named CLI profiles with protected credential storage and ship a standalone `sandbox` executable for sandbox inspection, creation, lifecycle operations, waiting, one-shot commands, and interactive shells.
