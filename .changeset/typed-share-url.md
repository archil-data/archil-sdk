---
"disk": patch
---

Use the generated public API types for `Disk.share()` and expose `activeDataBytes` and `totalDataBytes` on disks while retaining `dataSize` as a compatibility alias for `totalDataBytes`.
